services:
  mongodb:
    image: mongo:7.0
    container_name: news-portal-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=news-portal
    restart: unless-stopped

volumes:
  mongodb_data:
